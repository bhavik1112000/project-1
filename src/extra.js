// setCurrentProducts(allProducts.slice(startIndex, endIndex));
// setCurrentProducts(allProducts.filter(product)=>product.title.toLowerCase().includes(text.toLowerCase()).slice(startIndex, endIndex));
// filteredProducts = allProducts.filter((product) =>
//   product.title.toLowerCase().includes(text.toLowerCase())
// );
// console.log("filteredProducts", filteredProducts.length);
// setCurrentProducts(filteredProducts.slice(startIndex, endIndex));

// const removeActiveClass = () => {
//   const bullets = document.querySelectorAll(".pagination-bullet");
//   bullets.forEach((bullet) => bullet.classList.remove("active"));
// };

// const clickHandler = (e) => {
//   removeActiveClass();
//   e.target.classList.add("active");
// };
